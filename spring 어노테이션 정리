@Controller
-Component의 하위 클래스
-객체화시키고 기능(Controller)추가

@RequestMapping("이름")
-url에 "이름"을 지정
-"이름" 요청시 호출

@GetMapping("이름")
-get방식일때만 실행

@PostMapping("이름")
-post방식일때만 실행

@Service
-Component의 하위 클래스
-Service기능을 담당
-Controller + dao 중간 매개체 역활

@Size(min= ,max= ,message=" ")
-min:최소, max:최대, message: 에러 문구

@NotEmpty(message=" ")
-값이 없으면 에러 문구 출력

@Past(message=" ")
과거날짜인지 확인

@DateTimeFormat(pattern="yyyy-MM-dd")
-parrern형식의 날짜 형식

@Component	
-객체화

@Aspect		
-AOP 실행 클래스

@Order(1)	
-AOP 실행 순서

@Around()	
-기본메서드 실행 전, 후 실행

@Valid
- 유효성 검증이 필요한 객체임을 지정한다.

@RestController
- (@Controller + @ResponseBody) 합친내용
- View를 필요로하지 않고 데이터만 보낼때 사용(ajax 등)
- json, xml으로 사용가능

@Repository
- DAO 특화 어노테이션








